// Inizializzare la pagina e i dati dinamici (può essere usato per fetch dei dati in seguito)
document.addEventListener('DOMContentLoaded', function() {
    // Codice per caricare dinamicamente i dati se necessario
});
